apiVersion: apps/v1
kind: Deployment
metadata:
  name: kind-registry
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kind-registry
  template:
    metadata:
      labels:
        app: kind-registry
    spec:
      containers:
      - name: registry
        image: registry:2
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: kind-registry
  namespace: default
spec:
  selector:
    app: kind-registry
  ports:
  - port: 5000
    targetPort: 5000
