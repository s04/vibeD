{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vibed.serviceAccountName" . }}
  labels:
    {{- include "vibed.labels" . | nindent 4 }}
{{- end }}
